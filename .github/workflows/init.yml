name: 打包vue项目

on: push

jobs:
  npm-build:
    name: npm-bulid工作
    runs-on: ubuntu-latest

    steps:
      - name: 读取仓库目录
      - uses: actions/checkout@v4
      - run: npm install
      - run: npm run build
