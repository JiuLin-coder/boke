name: 检查一下配置
on: push

permissions: {}

jobs:
  npm-build:
    name: npm构建
    runs-on: ubuntu-latest

    steps:
      - name: 检查仓库内容
        use: actions/checkout@v4

      - name: npm检查
        run: npm --version
