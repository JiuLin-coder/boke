# boke

## 账本

### 获得数据

1.为了防止有人设置了超出每月最多天数的天，所以与其禁止，不如直接帮他们设好
数据条为对象：年月日为键，neiRong为每笔钱

2.我感觉如果说每一天都存储，那那些没有的天，却保存太浪费了。
动态展示是哪一天，点哪一天才能写入数据，
把有数据的存储
每一数据条为数组["dateName",neiRong,sum],而neiRong也是数组，每一项为每笔钱

3.然后在页面上显示所有天，但是只有那些有neiRong的才保存。
就是不如动态显示年月日，然后只保存需要的天数，后面显示，应该也是通过那些有的天数
要弄出暂存（就是先存在浏览器），保存（发送到后端）

> 我感觉我要是一直这么搞，绝对写不完，先实现功能，再迭代吧

这动态显示年月日与获取数据息息相关。

如何保存到后端，  
1.开服务器  
2.保存在浏览器的localStorage  
3.使用文件系统，修改文件
-1.使用浏览器API来处理
（但是浏览器的API有保护机制，要么使用旧的，但是只能查看和下载（虽然可以用同名下载覆盖方式，但是还需要手动确认）。要么使用新的）
-2.使用nodejs，自动的fs文件函数，但是每次都需要先启动才能保存


前端发送数据，清空数据 => 将数据条保存到数据库（这里是用本地文件替代）
